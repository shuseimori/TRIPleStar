<div class="col-md-8">
    <% if @user.microposts.any? %>
      <h3>Microposts (<%= @user.microposts.count %>)</h3>
      <ol class="microposts">
        <%= render partial: "microposts/microposts", :collections => @microposts %>
      </ol>
    <% end %>
  </div>